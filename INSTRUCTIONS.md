1. Download/install `go` `1.19`.
2. Run `make`.  The default task will download dependencies, compile, run tests,
etc and then start a running server listening on the port indicated by the
`PORT` env var (defaults to `3000`).
